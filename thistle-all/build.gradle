
dependencies {
    //required
    compile project(':thistle-core') { group project_group_id; version project_version }
    compile project(':thistle-crypto') { group project_group_id; version project_version }

    //optional

    //test
}
